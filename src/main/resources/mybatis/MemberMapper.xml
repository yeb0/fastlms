<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zerobase.fastlms.admin.mapper.MemberMapper">
    <select id="selectList" resultType="com.zerobase.fastlms.admin.dto.MemberDto">

        select *
        from member
        left outer join login_history
        on member.user_id = login_history.user_id;

    </select>
    <select id="selectList2" resultType="com.zerobase.fastlms.admin.dto.LoginHistoryDto">



    </select>
</mapper>